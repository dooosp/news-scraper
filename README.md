# 📰 뉴스 스크래퍼 & PPT 생성기

네이버 뉴스 IT/과학 섹션에서 헤드라인을 자동으로 수집하고, 분석 리포트와 PPT 발표 자료를 생성하는 Node.js 프로젝트입니다.

## ✨ 새로운 기능: 웹 인터페이스! 🎉

**리셋 버튼을 클릭하면 자동으로 뉴스를 새로 수집하고 PPT를 생성합니다!**

## 🚀 빠른 시작

### 1. 서버 실행
```bash
cd ~/news-scraper
export NVM_DIR="$HOME/.nvm" && [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
npm start
```

또는 간단하게:
```bash
cd ~/news-scraper
node server.js
```

### 2. 브라우저 접속
```
http://localhost:3000
```

### 3. 리셋 버튼 클릭
- 페이지의 **"🔄 뉴스 새로고침 & PPT 생성"** 버튼 클릭
- 자동으로 최신 뉴스 수집 및 PPT 생성
- 원하는 만큼 반복 가능!

## 🌟 주요 기능

### 1. 웹 인터페이스 (NEW!)
- **리셋 버튼**: 클릭 한 번으로 뉴스 재수집 & PPT 생성
- **실시간 표시**: 수집된 뉴스를 화면에 바로 표시
- **원클릭 다운로드**: Markdown, PowerPoint 파일 즉시 다운로드
- **반응형 디자인**: 모바일/데스크톱 모두 지원
- **실시간 로딩**: 작업 진행 상황 표시

### 2. 뉴스 수집
- 네이버 뉴스 IT/과학 섹션에서 실시간 헤드라인 5개 수집
- 제목, 요약, 링크 정보 자동 추출
- 리셋 버튼으로 언제든지 재수집

### 3. 데이터 저장
- **Markdown 형식**: 날짜별로 `archive/news_YYYY-MM-DD.md` 파일 생성
- **최신 뉴스**: `~/today_news.md` 파일에 항상 최신 뉴스 저장
- 리셋 버튼 클릭 시마다 자동 업데이트

### 4. 자동 분석
- 핵심 키워드 자동 추출 (빈도 분석)
- 트렌드 인사이트 생성
- AI, 모바일, 보안, 통신 등 주요 카테고리 분류

### 5. PPT 자동 생성
자동으로 4장의 슬라이드로 구성된 발표 자료를 생성합니다:
- **슬라이드 1**: 제목 페이지 (날짜 + 'IT 뉴스 트렌드 리포트')
- **슬라이드 2**: 오늘의 주요 IT 뉴스 헤드라인 5개
- **슬라이드 3**: 핵심 키워드 & 트렌드 인사이트
- **슬라이드 4**: 오늘의 요약

## 📦 설치

### 1. Node.js 설치 (이미 설치됨)
```bash
# nvm 로드
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"

# 버전 확인
node --version  # v24.12.0
```

### 2. 프로젝트 의존성 (이미 설치됨)
설치된 패키지:
- `axios`: HTTP 요청
- `cheerio`: HTML 파싱
- `pptxgenjs`: PowerPoint 생성
- `express`: 웹 서버

## 🎯 사용 방법

### 방법 1: 웹 인터페이스 (추천!)

1. **서버 시작**
   ```bash
   cd ~/news-scraper
   npm start
   ```

2. **브라우저 접속**
   ```
   http://localhost:3000
   ```

3. **리셋 버튼 클릭**
   - 원하는 만큼 반복 가능
   - 같은 날짜는 자동 업데이트
   - 다른 날짜는 새 파일 생성

4. **파일 다운로드**
   - 화면에서 바로 다운로드 가능

### 방법 2: CLI 명령어

웹 인터페이스 없이 직접 실행:
```bash
cd ~/news-scraper
npm run fetch
```

또는:
```bash
cd ~/news-scraper
node fetch-news.js
```

## 📁 프로젝트 구조

```
news-scraper/
├── server.js              # 웹 서버 (NEW!)
├── news-fetcher.js        # 뉴스 수집 모듈 (NEW!)
├── create-ppt.js          # PPT 생성 모듈
├── fetch-news.js          # CLI용 스크립트
├── package.json           # 프로젝트 설정
├── public/                # 웹 인터페이스 (NEW!)
│   └── index.html         # 웹 페이지
├── archive/               # 저장된 데이터
│   ├── news_2026-01-13.md
│   └── IT_News_Report_2026-01-13.pptx
├── README.md              # 프로젝트 설명서
└── HOW_TO_USE.md          # 상세 사용 가이드 (NEW!)
```

## 📊 생성되는 파일

### Markdown 파일 (`.md`)
- 날짜별 뉴스 헤드라인과 요약
- 링크 포함
- 한국어로 작성

### PowerPoint 파일 (`.pptx`)
- 4개의 슬라이드로 구성
- 프로페셔널한 디자인
- 키워드 분석 및 트렌드 인사이트 포함
- 즉시 발표 가능한 형식

## 🎨 웹 인터페이스 미리보기

```
┌─────────────────────────────────────────┐
│  📰 IT 뉴스 스크래퍼 & PPT 생성기       │
│  네이버 IT 뉴스를 자동으로 수집합니다   │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│  [🔄 뉴스 새로고침 & PPT 생성]          │
│  💡 버튼을 클릭하여 최신 뉴스 수집      │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│  5개 뉴스 | 📄 Markdown | 📊 PowerPoint│
│                                         │
│  오늘의 IT 뉴스                         │
│  ─────────────────────────────────      │
│  1. [뉴스 제목]                         │
│     요약: ...                           │
│     📰 기사 읽기 →                      │
│                                         │
│  📥 생성된 파일 다운로드                │
│  [📄 Markdown] [📊 PowerPoint]          │
└─────────────────────────────────────────┘
```

## 🔧 NPM 스크립트

```bash
npm start      # 웹 서버 시작
npm run fetch  # CLI로 뉴스 수집
```

## 🎯 사용 시나리오

### 시나리오 1: 매일 아침 뉴스 확인
1. 서버 실행: `npm start`
2. 브라우저 접속: `http://localhost:3000`
3. 리셋 버튼 클릭
4. 뉴스 확인 및 PPT 다운로드

### 시나리오 2: 발표 자료 준비
1. 웹에서 리셋 버튼 클릭
2. PPT 다운로드 버튼 클릭
3. PowerPoint에서 열어서 발표 준비
4. 필요시 수정 후 발표

### 시나리오 3: 트렌드 분석
1. 매일 리셋 버튼 클릭하여 데이터 축적
2. `archive/` 폴더에 날짜별 데이터 저장
3. 일주일/한달 치 데이터 분석
4. 키워드 트렌드 파악

## 📝 주의사항

1. **네이버 뉴스 구조 변경**: 네이버 웹사이트 구조가 변경되면 스크립트 수정이 필요할 수 있습니다.
2. **네트워크 연결**: 인터넷 연결이 필요합니다.
3. **같은 날짜 재실행**: 같은 날짜에 리셋 버튼을 여러 번 클릭하면 파일이 최신 뉴스로 업데이트됩니다.
4. **포트 사용**: 3000번 포트가 이미 사용 중이면 `server.js`에서 포트 변경이 필요합니다.

## 🙋 문제 해결

### 서버가 시작되지 않음
```bash
# Node.js 환경 로드
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"

# 다시 시작
npm start
```

### 포트가 이미 사용 중
```bash
# 3000번 포트 사용 프로세스 확인
lsof -i :3000

# 프로세스 종료
kill -9 [프로세스ID]
```

### 뉴스가 수집되지 않음
네이버 뉴스 웹사이트 구조가 변경되었을 가능성이 있습니다.

## 📚 더 알아보기

- 상세 사용 가이드: `HOW_TO_USE.md`
- 웹 서버 코드: `server.js`
- 뉴스 수집 로직: `news-fetcher.js`
- PPT 생성 로직: `create-ppt.js`

---

**Made with ❤️ by News Scraper Bot**

**🎉 이제 리셋 버튼으로 간편하게 사용하세요!**
