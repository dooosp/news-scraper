# Anti-Echo-Chamber 구현 계획서

## 1. 개요

**프로젝트명:** Anti-Echo-Chamber (확증편향 방지기)

**목적:** 뉴스 기사에 대한 반대 관점을 AI로 생성하여 확증편향에서 벗어나도록 돕는 범용 모듈

**핵심 가치:** "당신이 틀릴 수 있는 이유"를 브리핑

---

## 2. 아키텍처 결정 (ADR)

### 2.1 범용 모듈 설계

```
/home/taeho/
├── anti-echo-chamber/          # 독립 프로젝트 (범용 모듈)
│   ├── index.js                # 메인 진입점
│   ├── lib/
│   │   ├── perspective-generator.js   # 반대 관점 생성 (AI)
│   │   ├── news-filter.js             # 분석할 뉴스 선별
│   │   └── providers/
│   │       ├── gemini.js              # Gemini API
│   │       └── claude.js              # Claude API (확장용)
│   ├── templates/
│   │   └── email-section.js           # 이메일 섹션 템플릿
│   └── package.json
│
└── news-scraper/               # 기존 프로젝트
    └── (anti-echo-chamber 모듈 import)
```

### 2.2 왜 독립 프로젝트인가?

| 옵션 | 장점 | 단점 |
|------|------|------|
| **A) 독립 프로젝트** ✅ | 다른 에이전트에서 재사용 가능, 버전 관리 용이 | 초기 설정 약간 복잡 |
| B) news-scraper 내부 모듈 | 간단한 통합 | 재사용 어려움 |
| C) npm 패키지 배포 | 최고 재사용성 | 오버엔지니어링 |

**결정:** A) 독립 프로젝트 - 로컬 모듈로 import

---

## 3. 핵심 기능 설계

### 3.1 입력/출력 인터페이스

```javascript
// 입력: 뉴스 객체 (news-scraper 형식과 호환)
const newsItem = {
  title: "정부, 금리 인하 결정",
  summary: "한국은행이 기준금리를 0.25%p 인하...",
  sources: ["네이버", "연합뉴스"],
  category: "domestic"
};

// 출력: 반대 관점 분석
const counterPerspective = {
  originalTitle: "정부, 금리 인하 결정",
  originalClaim: "금리 인하로 경기 부양 효과 기대",
  counterArguments: [
    {
      point: "인플레이션 재발 우려",
      explanation: "금리 인하는 시중 유동성을 증가시켜..."
    },
    {
      point: "부동산 가격 상승 촉발",
      explanation: "저금리 환경에서 부동산 투기..."
    }
  ],
  alternativeViewpoint: "금리 인하보다 재정정책이 더 효과적이라는 견해도...",
  disclaimer: "AI 분석 기반"
};
```

### 3.2 AI 프롬프트 설계

```javascript
const SYSTEM_PROMPT = `
당신은 비판적 사고를 돕는 분석가입니다.
주어진 뉴스에 대해 반대 관점을 제시해주세요.

규칙:
1. 뉴스의 핵심 주장을 먼저 파악
2. 해당 주장에 대한 합리적인 반론 2-3개 제시
3. 대안적 관점 또는 누락된 맥락 제시
4. 감정적이지 않고 논리적으로 분석
5. "틀렸다"가 아니라 "다른 관점도 있다"는 톤 유지

출력 형식: JSON
`;
```

### 3.3 처리 흐름

```
┌─────────────────┐
│  뉴스 배열 입력  │
│  (news-scraper) │
└────────┬────────┘
         │
         ▼
┌─────────────────────┐
│  뉴스 필터링        │
│  - HOT 뉴스 우선    │
│  - 논쟁적 주제 선별 │
│  (최대 3-5개)       │
└────────┬────────────┘
         │
         ▼
┌─────────────────────┐
│  AI 분석 요청       │
│  (Gemini API)       │
│  - 배치 처리        │
│  - 병렬 요청        │
└────────┬────────────┘
         │
         ▼
┌─────────────────────┐
│  반대 관점 생성     │
│  - 핵심 주장 추출   │
│  - 반론 2-3개       │
│  - 대안적 관점      │
└────────┬────────────┘
         │
         ▼
┌─────────────────────┐
│  이메일 섹션 생성   │
│  HTML 템플릿 적용   │
└─────────────────────┘
```

---

## 4. 이메일 출력 형식 설계

### 4.1 섹션 위치

```
기존 이메일 구조:
┌─────────────────────────┐
│ 📰 오늘의 인기 뉴스     │
├─────────────────────────┤
│ 🔥 HOT 뉴스            │
├─────────────────────────┤
│ 🇰🇷 국내 주요 뉴스    │
├─────────────────────────┤
│ 📊 심층 분석 기사       │
├─────────────────────────┤
│ 🌍 글로벌 핫뉴스       │
├─────────────────────────┤
│ 🎭 다른 관점에서 보기   │  ← NEW!
├─────────────────────────┤
│ 🔄 새로고침 버튼       │
└─────────────────────────┘
```

### 4.2 섹션 디자인 (HTML 미리보기)

```
┌─────────────────────────────────────────────────┐
│ 🎭 다른 관점에서 보기                           │
│ ─────────────────────────────────────────────── │
│ 확증편향을 벗어나 다양한 시각을 경험해보세요     │
│                                                 │
│ ┌─────────────────────────────────────────────┐ │
│ │ 📰 "정부, 금리 인하 결정"                   │ │
│ │                                             │ │
│ │ 핵심 주장: 금리 인하로 경기 부양 효과 기대   │ │
│ │                                             │ │
│ │ 🤔 반론:                                    │ │
│ │ • 인플레이션 재발 우려                      │ │
│ │   금리 인하는 시중 유동성을 증가시켜...     │ │
│ │ • 부동산 가격 상승 촉발                     │ │
│ │   저금리 환경에서 부동산 투기 우려...       │ │
│ │                                             │ │
│ │ 💡 대안적 관점:                             │ │
│ │ 일부 경제학자들은 금리 인하보다 재정정책이   │ │
│ │ 더 효과적이라고 주장...                     │ │
│ │                                             │ │
│ │ ⚠️ AI 분석 기반 | 투자/의사결정용 아님     │ │
│ └─────────────────────────────────────────────┘ │
│                                                 │
│ (2-3개 더 반복)                                 │
└─────────────────────────────────────────────────┘
```

### 4.3 색상 팔레트

| 요소 | 색상 | 의미 |
|------|------|------|
| 섹션 테두리 | `#6c5ce7` (보라) | 중립적, 사려깊은 |
| 반론 강조 | `#d63031` (빨강) | 주의, 경고 |
| 대안 관점 | `#0984e3` (파랑) | 새로운 시각 |
| 면책 조항 | `#b2bec3` (회색) | AI 분석임을 명시 |

---

## 5. 기술 스택

### 5.1 의존성

```json
{
  "name": "anti-echo-chamber",
  "version": "1.0.0",
  "main": "index.js",
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "dotenv": "^17.2.3"
  }
}
```

### 5.2 환경 변수

```bash
# anti-echo-chamber/.env 또는 news-scraper/.env
GEMINI_API_KEY=your_api_key_here
```

---

## 6. 파일 구조 상세

```
anti-echo-chamber/
├── package.json
├── index.js                      # 메인 export
│
├── lib/
│   ├── perspective-generator.js  # 핵심 로직
│   │   └── generateCounterPerspective(newsItem)
│   │   └── generateBatch(newsArray, options)
│   │
│   ├── news-filter.js            # 분석할 뉴스 선별
│   │   └── selectForAnalysis(newsArray, maxCount)
│   │
│   └── providers/
│       └── gemini.js             # Gemini API 래퍼
│           └── analyze(prompt)
│
├── templates/
│   └── email-section.js          # HTML 생성
│       └── renderSection(perspectives)
│       └── renderSingleItem(perspective)
│
└── examples/
    └── usage.js                  # 사용 예제
```

---

## 7. news-scraper 통합 계획

### 7.1 수정할 파일

| 파일 | 수정 내용 |
|------|-----------|
| `github-action-sender.js` | anti-echo-chamber 모듈 import 및 호출 |
| `package.json` | 로컬 모듈 의존성 추가 |
| `.env` | GEMINI_API_KEY 추가 |
| GitHub Secrets | GEMINI_API_KEY 추가 |

### 7.2 통합 코드 (예시)

```javascript
// github-action-sender.js 수정
const antiEcho = require('../anti-echo-chamber');

async function sendNewsEmail(headlines) {
  // 1. 기존: 뉴스 수집 완료

  // 2. NEW: 반대 관점 생성
  const perspectives = await antiEcho.generateBatch(headlines, {
    maxItems: 3,           // 최대 3개 뉴스 분석
    preferHot: true        // HOT 뉴스 우선
  });

  // 3. 이메일 HTML 생성 시 새 섹션 추가
  const antiEchoSection = antiEcho.templates.renderSection(perspectives);

  // 4. 기존 이메일에 삽입 (새로고침 버튼 앞)
}
```

---

## 8. Edge Cases & 리스크

### 8.1 기술적 리스크

| 리스크 | 완화 방안 |
|--------|-----------|
| Gemini API 실패 | try-catch + 그레이스풀 폴백 (섹션 생략) |
| API 비용 | 분석 뉴스 수 제한 (3개) |
| 응답 지연 | 타임아웃 설정 (30초) |
| JSON 파싱 실패 | 재시도 + 구조화된 프롬프트 |

### 8.2 콘텐츠 리스크

| 리스크 | 완화 방안 |
|--------|-----------|
| AI 환각 (hallucination) | 면책 조항 명시 |
| 민감한 주제 | "다양한 의견 존재" 수준으로 중립 유지 |
| 잘못된 반론 | "AI 분석 기반" 라벨 필수 |

---

## 9. 테스트 계획

### 9.1 단위 테스트

```
1. 단일 뉴스 → 반대 관점 생성 성공
2. 빈 뉴스 배열 → 빈 결과 반환
3. API 실패 → 그레이스풀 폴백
4. 영문 뉴스 (BBC/CNN) → 한글 반론 생성
```

### 9.2 통합 테스트

```bash
# 로컬에서 전체 플로우 테스트
cd news-scraper
node github-action-sender.js --test
```

---

## 10. 구현 순서 (Phase 4 - 승인 후)

| Step | 작업 | 예상 줄 수 |
|------|------|-----------|
| 1 | 프로젝트 초기화 (package.json) | ~15줄 |
| 2 | lib/providers/gemini.js | ~40줄 |
| 3 | lib/news-filter.js | ~30줄 |
| 4 | lib/perspective-generator.js | ~50줄 |
| 5 | templates/email-section.js | ~50줄 |
| 6 | index.js (export) | ~20줄 |
| 7 | news-scraper 통합 | ~30줄 수정 |

**총 예상:** ~235줄 (50줄 단위 × 5회 커밋)

---

## 11. 자가 비판 (Phase 3)

### 이 설계의 취약점

1. **AI 의존성**
   - Gemini API 다운 시 전체 기능 불가
   - → 폴백: 섹션 자체를 생략하고 기존 이메일만 발송

2. **비용 예측**
   - Gemini API는 무료 티어 있음 (15 RPM, 1M TPM)
   - 하루 1회, 3개 뉴스 분석 = 무료 한도 내

3. **반론 품질**
   - AI가 피상적인 반론만 생성할 수 있음
   - → 프롬프트에 "구체적 근거 포함" 지시

4. **확장성**
   - Claude API 추가 시 provider 파일만 추가하면 됨
   - 인터페이스 통일로 교체 용이

---

## 12. 승인 요청 사항

1. ✅ 독립 프로젝트 아키텍처
2. ✅ 이메일 섹션 위치 (글로벌 뉴스 다음, 새로고침 버튼 앞)
3. ✅ 분석 대상: HOT 뉴스 우선 최대 3개
4. ✅ AI Provider: Gemini (무료 티어 활용)

---

**"승인" 또는 "LGTM"을 입력하시면 구현을 시작합니다.**

---

*작성일: 2026-01-27*
*작성자: Claude (Staff Engineer Persona)*
